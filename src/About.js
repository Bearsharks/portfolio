import './About.scss';

function About(props) {
    console.log(props);
    console.log(window.globalState.app);
    return (
        <div>
            About
        </div>
    );
}

export default About;
